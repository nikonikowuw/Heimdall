# 烟火检测算法包配置

# 模型路径（可选，默认 model/best_pure.rknn）
# 支持相对路径（相对于算法包根目录）或绝对路径
# MODEL_PATH=model/best_pure.rknn

# 检测参数
CONF_THRESH=0.45
IOU_THRESH=0.45

# 本地测试图片（run_local 使用）
INPUT_IMAGE=testimage.jpg
OUTPUT_IMAGE=result.jpg

# 压力测试参数（run_local 使用）
LOOPS=100
WARMUP=5
DURATION=30
